// Settings to be passed from CMake via a configure_file call.
#define CMakeDemo_VERSION_MAJOR @CMakeDemo_VERSION_MAJOR@
#define CMakeDemo_VERSION_MINOR @CMakeDemo_VERSION_MINOR@

// Should we use the custom math library?
#cmakedefine USE_MYMATH

// Does the platform provide exp and log functions?
#cmakedefine HAVE_LOG
#cmakedefine HAVE_EXP